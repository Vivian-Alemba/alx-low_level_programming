#include "main.h"
#include <stdlib.h>

/**
 * _abs - a function that computes the absolute value of integer
 *@n: Shows the character to be printed
 *
 * Return: (n);
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = n * -1;
	}
	return (n);

}
