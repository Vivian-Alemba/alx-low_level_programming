#include "main.h"

/**
 * mul - a function that multiplies two integers.
 * @a: Character to be printed.
 * @b: Character to be printed.
 *
 * Return: result
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
